import { NoRippleDirective } from './no-ripple.directive';

describe('NoRippleDirective', () => {
  it('should create', () => {
    const directive = new NoRippleDirective();
    expect(directive).toBeTruthy();
  });
});
